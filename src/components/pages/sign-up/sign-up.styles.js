import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  buttonContainer: {
    display: 'flex',
    justifyContent: 'flex-end',
  },
  signInButton: {
    textTransform: 'none',
  },
}));
